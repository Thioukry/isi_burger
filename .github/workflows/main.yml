name: ISI Burger CI
on:
  push:
    branches: [ "dieumbe_burger" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker Image
        run: docker build -t isi-burger-app:latest .

